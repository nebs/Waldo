= render 'shared/breadcrumbs',
         breadcrumbs: { 'Areas' => nil }
.areas
  - @areas.each do |area|
    %section.area-section
      %header= link_to area.name, area_path(area)
      = render 'areas/map', area: area
